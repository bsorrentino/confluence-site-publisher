language: node_js
node_js:
  - "8.9.4"
#before_install:
#- npm i -g makeshift && makeshift -s @npm -r https://registry.npmjs.org
services:
  - docker
deploy:
  provider: npm
  email: bartolomeo.sorrentino@gmail.com
  api_key: $NPM_TOKEN
  on:
    tags: true